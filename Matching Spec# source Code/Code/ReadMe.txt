class "MyVisitor" is used to get parameter information;
class "VisitorForRename" is used to do the process of rename;
class "OutputVisitor" is used for print out;
class "OutputContractVisitor" is used for print out as well, while using the method of text operate;
class "main"
These five classes above are in the same namespace;

class "Passer" is used to get the AST

class "CodePrinter" is used to print out the form we expect (the code between //ll  --- //end  is written by me);

class "Compiler" is used to get attribute list (the code between //ll  --- //end  is written by me);




